# buildah -t lumosql-fedora
FROM fedora:31

RUN true \
  && dnf --assumeyes update \
  && dnf install --assumeyes \
    git make gcc ncurses-devel readline-devel glibc-devel autoconf tcl-devel \
  && dnf clean all \
  && rm -rf /var/cache/yum \

